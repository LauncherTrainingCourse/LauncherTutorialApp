<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/activity_contact_info"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="horizontal">

    <android.support.v7.widget.Toolbar
        android:id="@+id/contact_info_toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="@android:color/holo_blue_bright"
        android:elevation="4dp"
        android:theme="@style/ThemeOverlay.AppCompat.ActionBar"
        app:popupTheme="@style/ThemeOverlay.AppCompat.Light" />

    <ImageView
        android:id="@+id/contact_info_photo"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_below="@id/contact_info_toolbar"
        android:adjustViewBounds="true"
        android:scaleType="fitStart" />

    <TextView
        android:id="@+id/contact_info_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/contact_info_toolbar"
        android:layout_toRightOf="@id/contact_info_photo"
        android:paddingLeft="20dp"
        android:paddingTop="15dp"
        android:textColor="@color/contactName"
        android:textSize="30dp" />

    <TextView
        android:id="@+id/contact_info_phone"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/contact_info_name"
        android:layout_toRightOf="@id/contact_info_photo"
        android:paddingLeft="20dp"
        android:paddingTop="15dp"
        android:textSize="24dp" />

    <TextView
        android:id="@+id/contact_info_company"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/contact_info_phone"
        android:layout_toRightOf="@id/contact_info_photo"
        android:paddingLeft="20dp"
        android:paddingTop="15dp"
        android:textSize="20dp" />

    <TextView
        android:id="@+id/contact_info_email"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/contact_info_company"
        android:layout_toRightOf="@id/contact_info_photo"
        android:paddingLeft="20dp"
        android:paddingTop="15dp"
        android:textSize="20dp" />
</RelativeLayout>
